after loads of fighting with all this, this easiest way is to get tiles off 'versatiles' and use their server to serve them. the layer is called 'bathymetry' and the key is 'mindepth'

then it's just `your/server/tiles/[filename]/{z}/{x}/{y}` (with no extension)
